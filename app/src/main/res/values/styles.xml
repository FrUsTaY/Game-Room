<resources>
    <!-- Базовая тема приложения (без ActionBar) -->
    <style name="Theme.AppCompat.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Тема для Splash Screen -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Цвет фона заставки -->
        <item name="windowSplashScreenBackground">#08090d</item>
        <!-- Иконка в центре заставки (можно заменить на свою) -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <!-- Тема, которая применится после скрытия заставки -->
        <item name="postSplashScreenTheme">@style/Theme.AppCompat.NoActionBar</item>
    </style>
</resources>